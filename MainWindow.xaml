<Window x:Class="RecipeUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RecipeUI"
        mc:Ignorable="d"
        Title="Receptes könyv" Height="450" Width="800" Icon="/chef_ingredients_restaurant_and_food_recipe_book_cook_icon_251527.ico">
    <Grid>
        <TabControl>
            <TabItem Header="Receptek">
                <Grid Background="#FFE5E5E5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <ListView x:Name="RecipeList" Grid.Column="0" Margin="10,13,0,10" d:ItemsSource="{d:SampleData ItemCount=5}" Width="180">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="ID"  Width="30" DisplayMemberBinding="{Binding ID}" />
                                <GridViewColumn Header="Receptek"  Width="150" DisplayMemberBinding="{Binding Name}" />
                            </GridView>
                        </ListView.View>
                    </ListView>
                    <DataGrid x:Name="RecipeGrideView" Grid.Column="1" Margin="8,13,10,10" d:ItemsSource="{d:SampleData ItemCount=10}"/>
                </Grid>
            </TabItem>
            <TabItem Header="Hűtő">
                <Grid Background="#FFE5E5E5">
                    <DataGrid x:Name="RefrigeratorStock" Grid.Column="1" Margin="10,58,10,10" d:ItemsSource="{d:SampleData ItemCount=10}"/>
                    <Button x:Name="AddItem_RS" Content="Hozzáadás" HorizontalAlignment="Left" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Height="33" Width="100" Margin="10,10,0,0" VerticalAlignment="Top"/>
                </Grid>
            </TabItem>
            <TabItem Header="Hozzávalók">
                <Grid Background="#FFE5E5E5">
                    <DataGrid x:Name="ItemDB" Grid.Column="1" Margin="10,58,10,10" d:ItemsSource="{d:SampleData ItemCount=10}"/>
                    <Button x:Name="AddItem_DB" Content="Hozzáadás" HorizontalAlignment="Left" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Height="33" Width="100" Margin="10,10,0,0" VerticalAlignment="Top"/>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
